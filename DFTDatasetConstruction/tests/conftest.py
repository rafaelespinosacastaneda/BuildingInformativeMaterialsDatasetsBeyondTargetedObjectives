import sys
import os

# Add the project root to the Python path so that src modules can be imported
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src', 'multiob-al'))